import React, { FC } from "react";
import SettingsStyle from "./Settings.module.css";

const Settings: FC = () => {
    return <div className={SettingsStyle.wrapper}>It's SETTINGS</div>;
};

export default Settings;
